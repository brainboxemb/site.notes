# Docsify

Docsify is used to publish this knowledge base directly from Markdown files.

Unlike a traditional static site generator, Docsify does not generate HTML files during a build step. Markdown files are loaded and rendered in the browser.

